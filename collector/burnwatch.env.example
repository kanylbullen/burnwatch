# Copy to burnwatch.env beside this file (Windows) or to ~/.burnwatch/env
# (Linux/macOS), then fill in the values printed by install/setup.sh.
#
# Keep the real file out of version control - it holds your daemon token.

BURNWATCH_URL=http://127.0.0.1:8787
BURNWATCH_TOKEN=
